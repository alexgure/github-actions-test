name: ENV variables

on: push
env:
  WF_ENV: Workflow var


jobs:
  log-env:
    runs-on: ubuntu-latest
    env:
      JOB_ENV: Job var     
    steps:
      - name: Log ENV Variables
        env:
          STEP_ENV: Step var
        run: |
          echo "WF_ENV: ${WF_ENV}"
          echo "JOB_ENV: ${JOB_ENV}"
          echo "STEP_ENV: ${STEP_ENV}"
      - name: Log env 2
        run: |
          echo "WF_ENV: ${WF_ENV}"
          echo "JOB_ENV: ${JOB_ENV}"
          echo "STEP_ENV: ${STEP_ENV}"
  log-default-env:
    runs-on: ubuntu-latest
    steps:
      - name: Get all vars
        run: env|grep GIT    
            
        

            
